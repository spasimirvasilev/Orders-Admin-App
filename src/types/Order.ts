type Order = {
  id: string;
  created: string;
  status: string;
  customer: string;
  sku: string;
  photo: string;
  condition: string;
  size: string;
  type: string;
  origin_address: string;
  shipping_address: string;
};

export default Order;
